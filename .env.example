# Server configuration
NODE_ENV=production
SERVER_PORT=6006
LOG_LEVEL=info

# Accepted values: error, warn, info, http, verbose, debug, silly

# Cross-origin resource sharing
CORS_ALLOWED_ORIGINS=http://localhost:6007,http://192.168.1.8:6007,https://projectx.dodon.in

# PostgreSQL connection string
DATABASE_URL=postgres://sayabasu:%211Dilbert@192.168.1.3:5432/project-x-dev

# MinIO connection details
MINIO_ENDPOINT=cdn.dodon.in
MINIO_PORT=443
MINIO_USE_SSL=true
MINIO_ACCESS_KEY=sayabasu
MINIO_SECRET_KEY=!1Dilbert
MINIO_REGION=us-east-1
MINIO_BUCKET=project-x-dev-bucket
MINIO_PRESIGNED_URL_EXPIRATION_SECONDS=3600

# Email / SMTP
EMAIL_FROM=aami@dodon.in
EMAIL_SMTP_HOST=smtp.zoho.com
EMAIL_SMTP_PORT=465
EMAIL_SMTP_SECURE=true
EMAIL_SMTP_USER=aami@dodon.in
EMAIL_SMTP_PASS=fPgCjy6HdBNh

# Authentication / User management
AUTH_ACCESS_TOKEN_SECRET=replace-with-long-random-string
AUTH_REFRESH_TOKEN_SECRET=replace-with-another-long-string
AUTH_ACCESS_TOKEN_TTL_SECONDS=900
AUTH_REFRESH_TOKEN_TTL_SECONDS=604800
AUTH_PASSWORD_RESET_TOKEN_TTL_MINUTES=30
AUTH_EMAIL_VERIFICATION_TOKEN_TTL_MINUTES=1440
AUTH_PASSWORD_SALT_ROUNDS=12
APP_BASE_URL=http://localhost:6007
AUTH_DEFAULT_ADMIN_EMAIL=admin@example.com
AUTH_DEFAULT_ADMIN_PASSWORD=please-change-me-123
AUTH_DEFAULT_ADMIN_NAME=Project X Administrator

# Client configuration
CLIENT_PORT=6007
CLIENT_ALLOWED_HOSTS=projectx.dodon.in,localhost,192.168.1.3
# Set to true whenever the Vite dev server is proxied through HTTPS (e.g., Cloudflare).
# Leave false for local HTTP development so browsers can connect over ws.
CLIENT_USE_SECURE_HMR=false
# Optional override for the dev server HMR protocol (accepted values: ws, wss)
# CLIENT_HMR_PROTOCOL=wss
VITE_API_URL=http://localhost:6006
VITE_LOG_LEVEL=info
# Accepted values: error, warn, info, debug
